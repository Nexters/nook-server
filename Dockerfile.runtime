FROM eclipse-temurin:25-jre-alpine
WORKDIR /app

RUN addgroup -S nook && adduser -S nook -G nook
COPY --chown=nook:nook *.jar app.jar

USER nook
EXPOSE 8080
ENV JVM_OPTS="-XX:MaxRAMPercentage=75.0"

ENTRYPOINT ["sh", "-c", "exec java $JVM_OPTS -jar app.jar"]
