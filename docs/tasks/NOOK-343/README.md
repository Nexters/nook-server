# NOOK-343 사용자 행동 분석 이벤트 및 어드민 대시보드

## 목적

서버 API에서 확인 가능한 사용자 행동을 일관된 이벤트로 기록하고 가입, 저장 활성화, 재방문 흐름을
어드민 시각화 대시보드에서 확인한다.

## 범위

- 신규 회원 생성, 게시물 저장, 명시적 장소 저장 성공을 이벤트로 기록한다.
- 아카이브 첫 페이지, 게시물 상세, 장소 상세, 지도 영역 조회 성공을 이벤트로 기록한다.
- 이벤트는 사용자·대상·일자 기준으로 중복을 제거한다.
- 가입 cohort의 저장 활성화와 D1, D7, D30 재방문을 집계한다.
- 선택 종료일 기준 DAU, 최근 7일 WAU, 최근 30일 MAU를 집계한다.
- 어드민에서 기간과 활성화 저장 개수를 선택하고 퍼널, 일별 추이, 재방문 행동을 시각적으로 확인한다.

## 제외 범위

- 앱 최초 실행 `first_open`
- 지도에서 실제 장소를 선택한 `map_place_select`
- 모바일 클라이언트 계측, 범용 수집 API와 외부 분석 SaaS 연동
- 기존 데이터의 이벤트 백필

## 이벤트 기준

- `sign_up`: 기존 로그인과 구분되는 신규 회원 생성
- `post_save`: 새로운 사용자 저장 게시물 생성 또는 삭제 상태 복원
- `place_save`: 사용자 요청으로 장소 북마크가 미저장 상태에서 저장 상태로 전환
- `archive_view`: 저장 게시물 또는 그룹 콘텐츠 첫 페이지 조회 성공
- `post_view`: 사용자가 소유한 저장 게시물 상세 조회 성공
- `place_view`: 사용자가 접근할 수 있는 장소 상세 조회 성공
- `map_view`: 사용자의 지도 영역 조회 성공

게시물 저장 과정에서 자동 생성되는 장소 북마크는 `place_save`로 기록하지 않는다.

`sign_up`, `post_save`, `place_save`는 회원·대상별 평생 1회, 조회 이벤트는 회원·대상·한국 날짜별 1회로
중복 제거한다. `map_view`는 실제 장소 선택이 아니라 지도 API를 통한 탐색의 대체 지표다.

## 후속 검토

- GA SDK 또는 범용 클라이언트 이벤트 API로 `first_open`, 정확한 `map_place_select`를 수집한다.
- 클라이언트 계측을 도입하면 익명 설치 식별자와 로그인 회원을 연결하는 표준을 별도로 정의한다.

## 검증

- 이벤트별 성공 및 중복 제거 단위 테스트
- 저장 상태가 실제로 변경된 경우에만 저장 이벤트가 기록되는지 검증
- 가입 cohort, 활성화 N개, exact-day retention 집계 테스트
- 서버 관측 이벤트 기반 DAU, WAU, MAU 집계 테스트
- 어드민 API controller 테스트
- `./gradlew check`
- `pnpm build`
- Chrome에서 기간·활성화 필터와 반응형 시각화 확인
