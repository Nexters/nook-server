services:
  worker:
    image: ${IMAGE}
    container_name: nook-live-worker
    restart: unless-stopped
    mem_limit: 768m
    mem_reservation: 512m
    memswap_limit: 1g
    env_file:
      - .env
    environment:
      APP_ENV: live
      SPRING_PROFILES_ACTIVE: live
      TZ: Asia/Seoul
      JVM_OPTS: "-Xms128m -Xmx384m -Duser.timezone=Asia/Seoul"
      MANAGEMENT_PORT: 8080
      GOOGLE_APPLICATION_CREDENTIALS: /run/secrets/firebase-service-account.json
      PLACE_PARSING_WORKER_CONCURRENCY: 2
      POST_CONTENT_PARSING_WORKER_CONCURRENCY: 2
    volumes:
      - ./secrets/aws-credentials:/run/secrets/aws_credentials:ro
      - ./secrets/aws-config:/run/secrets/aws_config:ro
      - ./secrets/firebase-service-account.json:/run/secrets/firebase-service-account.json:ro
    ports:
      - "192.168.0.216:18081:8080"
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    stop_grace_period: 70s

  parsing-alerts:
    image: python:3.13-alpine
    container_name: nook-live-parsing-alerts
    restart: unless-stopped
    command: ["python", "/app/forward_error_logs.py"]
    environment:
      ERROR_LOG_CONTAINER_NAME: nook-live-worker
      ERROR_LOG_ENV: live
      ERROR_LOG_PARSING_ONLY: "true"
      ERROR_LOG_DISCORD_WEBHOOK_URL: ${PARSING_ALERT_DISCORD_WEBHOOK_URL:?}
    volumes:
      - ./scripts/forward_error_logs.py:/app/forward_error_logs.py:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
